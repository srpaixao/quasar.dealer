namespace QuasarApi.Routes.Management
{
    public static class NotaFiscalRoutes
    {
        public static WebApplication MapNotaFiscalRoutes(this WebApplication app, WebApplicationBuilder builder)
        {
            // app.MapGet(pattern: "/notafiscal", handler: () => "Hello World from Nota Fiscal");
            // app.MapPost(pattern: "/notafiscal", handler: () => "Hello World from Nota Fiscal");
            // app.MapPut(pattern: "/notafiscal", handler: () => "Hello World from Nota Fiscal");
            // app.MapDelete(pattern: "/notafiscal", handler: () => "Hello World from Nota Fiscal");
            return app;
        }
    }
}

